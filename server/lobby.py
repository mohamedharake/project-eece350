"""
server/lobby.py

Reserved module: lobby and matchmaking logic is implemented in ``net_server``.

This file remains as a placeholder so the package layout matches the course
design document; production code paths use ``ServerState`` and ``handle_client``
in ``net_server`` instead.
"""

from __future__ import annotations

